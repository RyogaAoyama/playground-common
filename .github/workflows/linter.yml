name: Testing
on: push

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Docker build
        run: make docker-build
      - name: cfn linter
        run: make cfn-lint
